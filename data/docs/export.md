# Exporting your data
Admins can export all workspace data as a ZIP archive from Settings, Data, Export.
Exports include files, comments and an audit log in JSON format.
Large exports are prepared in the background and a download link is emailed when ready.
Export links stay valid for 7 days.
